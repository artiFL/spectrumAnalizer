#pragma once
#ifndef HEADER_H
	#define HEADER_H
#endif

#define CLOCK			20000000					//osc clock freq

typedef struct {
	uint16_t CLI_bacgroundeColor;
	uint16_t CLI_textColor;
	uint32_t tecc;
} config_t;
config_t config;


